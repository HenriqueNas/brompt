# Form Feature

This directory contains the core logic for the dynamic form engine powered by LLM-generated JSON schemas.

## Responsibilities

- Rendering form components based on JSON schema.
- Managing form state and validation.
- Handling user input and interactions.

## Key Components

- `DynamicForm`: The main component that renders the form.
- `FieldRenderer`: Renders individual fields based on type.
